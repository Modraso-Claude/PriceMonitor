name: Мониторинг цен Wildberries

on:
  workflow_dispatch: {}   # запускается вручную кнопкой в GitHub ИЛИ внешним
                          # планировщиком cron-job.org через GitHub API —
                          # встроенный schedule: GitHub Actions убран, т.к.
                          # ненадёжно пропускал запуски на бесплатном тарифе

permissions:
  contents: write

jobs:
  check-prices:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run price monitor
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python price_monitor.py

      - name: Commit updated price history
        run: |
          git config user.name "price-bot"
          git config user.email "price-bot@users.noreply.github.com"
          git add history.json
          git diff --staged --quiet || git commit -m "Обновление истории цен $(date -u +%F' '%H:%M)"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Попытка $i не удалась, повтор через 5 секунд..."
            sleep 5
          done
